#pragma once

#include <stdint.h>

namespace core {

	template <typename K, typename V>
	class VecMap {
	public:
		size_t size() const {
			return 0;
		}

		bool empty() const {
			return true;
		}

	private:
	};

} // namespace core
